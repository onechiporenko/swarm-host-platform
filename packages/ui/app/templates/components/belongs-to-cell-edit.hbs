<PowerSelect
  @selected={{get @record @column.propertyName}}
  @options={{this.options}}
  @searchField="id"
  @onChange={{this.updateCellValue}}
  as |option|
>
  {{option.id}}
</PowerSelect>
{{yield}}