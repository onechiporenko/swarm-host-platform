import { Lair } from '@swarm-host/server';
import { expect } from 'chai';
import <%= className %>Factory from '<%= importPath %>';

let lair;
describe('#<%= className %>Factory', () => {
  beforeEach(() => {
    Lair.cleanLair();
    lair = Lair.getLair();
    lair.registerFactory(<%= className %>Factory);
  });

  afterEach(() => {
    Lair.cleanLair();
  });

  describe('should have meta', () => {
    it('defined', () => {
      expect(lair.getMetaFor('<%= name %>')).not.to.be.undefined;
    });
  });
});
